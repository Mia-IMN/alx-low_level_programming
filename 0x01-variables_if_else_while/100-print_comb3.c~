#include <stdio.h>

/**
 *main - prints numbers with space and comma inbetween
 *
 *Return: 0
 */
int main(void)
{
int a;
int b = 0;
for (a = b + 1; a < 10; ++a)
{
for (b = a + 1; b < 10; ++b)
{
putchar((a % 10) + '0');
putchar((b % 10) + '0');
if (a == 8 && b == 9)
continue;
putchar(',');
putchar(' ');
}
}
putchar('\n');
return (0);
}
