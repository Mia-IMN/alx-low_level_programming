#include <ctype.h>
#include <stdio.h> 
#include <stdbool.h>

/**
*_islower - checks for lowercases
*
*Return: 0
*/
int _islower(int c)
{
if (c > 96 && c < 123)
{
putchar('1');
}
else
{
putchar('0');
}
return (0);
}
